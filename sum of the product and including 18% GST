/*enter cpst of 3 items from the user (using float data type)- a pencil, a pen and an eraser. you have to output  the total items back to the user as their bills */
import java.util.*;
public class JavaBasics{
    public static void main(String args[]){
        Scanner sc = new Scanner (System.in);
        float pencil = sc.nextFloat();
        float pen = sc.nextFloat();
        float eraser = sc.nextFloat();
        float Bill = ((pencil + pen + eraser) + (pencil + pen + eraser) * 0.18f);
        System.out.println(Bill);

    }

    
}
